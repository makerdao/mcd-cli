#!/usr/bin/env bash
### mcd-bites -- Recent bites
### Usage: mcd bites [-B <block>]
set -e

abi="event Bite(bytes32 indexed ilk,bytes32 indexed urn,uint256 ink,uint256 art,uint256 tab,uint256 flip);"
export SETH_ABI=$(seth abi "$abi")
seth events $MCD_CAT
